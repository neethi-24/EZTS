N = int(input()) # number of moves 
A = list(map(int, input().split())) # ant move's
count = 0
for i in range(N):
    if (sum(A[:i+1]) == 0): # original postion is at 0
        count = count + 1
print(count)
